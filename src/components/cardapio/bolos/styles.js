import tw from 'tailwind-styled-components'

export const SLabel = tw.label`mt-10 text-center text-3xl font-medium animate-fade-left animate-duration-[400ms] animate-ease-in animate-fill-forwards ${({
	className,
}) => className}`
